<controls:BasePickerPage
    x:Class="SimpleTasks.Views.ReminderPickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:SimpleTasks.Controls"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    Style="{StaticResource BasePickerPageStyle}">

    <controls:BasePickerPage.Resources>

        <Style x:Key="TimeTextStyle" TargetType="TextBlock" BasedOn="{StaticResource NormalTextStyle}">
            <Setter Property="Foreground" Value="{StaticResource TimeForegroundSubtleBrush}" />
            <Setter Property="FontSize" Value="56" />
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
    </controls:BasePickerPage.Resources>

    <Grid Style="{StaticResource BasePickerPageRootStyle}">
        <TextBlock Style="{StaticResource BasePickerPageHeaderStyle}" 
                   Text="{Binding LocalizedResources.ChooseReminderTime, Source={StaticResource LocalizedStrings}}" />

        <StackPanel VerticalAlignment="Bottom" 
                    Margin="0,0,0,20">

            <Grid Margin="0,15,40,-10" HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource TimeTextStyle}">
                    <Run Text="{Binding RoundValue, ElementName=DaySlider}" Foreground="{StaticResource TimeForegroundBrush}"/><Run Text="{Binding LocalizedResources.DateTimeDaysAbbr, Source={StaticResource LocalizedStrings}, Mode=OneTime}"/>
                </TextBlock>
                <TextBlock Grid.Column="1" Style="{StaticResource TimeTextStyle}">
                    <Run Text="{Binding RoundValue, ElementName=HourSlider}" Foreground="{StaticResource TimeForegroundBrush}"/><Run Text="{Binding LocalizedResources.DateTimeHoursAbbr, Source={StaticResource LocalizedStrings}, Mode=OneTime}"/>
                </TextBlock>
                <TextBlock Grid.Column="2" Style="{StaticResource TimeTextStyle}">
                    <Run Text="{Binding RoundValue, ElementName=MinuteSlider}" Foreground="{StaticResource TimeForegroundBrush}" /><Run Text="{Binding LocalizedResources.DateTimeMinutesAbbr, Source={StaticResource LocalizedStrings}, Mode=OneTime}"/>
                </TextBlock>
            </Grid>
            <TextBlock Text="{Binding LocalizedResources.ReminderBeforeDueDateText, Source={StaticResource LocalizedStrings}, Mode=OneTime}" Style="{StaticResource NormalTextStyle}" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="{StaticResource TimeBeforeTextBrush}" />

            <Grid Margin="0,0,0,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <controls:MySlider Grid.Row="2" x:Name="MinuteSlider" Label="{Binding LocalizedResources.MinutesLabel, Source={StaticResource LocalizedStrings}, Mode=OneTime}" Maximum="59" Margin="30,0">
                    <controls:MySlider.Background>
                        <SolidColorBrush Color="{StaticResource PhoneAccentColor}" Opacity="1"/>
                    </controls:MySlider.Background>
                </controls:MySlider>
                <controls:MySlider Grid.Row="1" x:Name="HourSlider" Label="{Binding LocalizedResources.HoursLabel, Source={StaticResource LocalizedStrings}, Mode=OneTime}" Maximum="23" Margin="30,0,30,-60" Mask="#33FFFFFF">
                    <controls:MySlider.Background>
                        <SolidColorBrush Color="{StaticResource PhoneAccentColor}" Opacity="0.7"/>
                    </controls:MySlider.Background>
                </controls:MySlider>
                <controls:MySlider Grid.Row="0" x:Name="DaySlider" Label="{Binding LocalizedResources.DaysLabel, Source={StaticResource LocalizedStrings}, Mode=OneTime}" Maximum="30" Margin="30,0,30,-60" Mask="#66FFFFFF">
                    <controls:MySlider.Background>
                        <SolidColorBrush Color="{StaticResource PhoneAccentColor}" Opacity="0.42"/>
                    </controls:MySlider.Background>
                </controls:MySlider>
            </Grid>

            <Grid VerticalAlignment="Bottom" Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel >
                    <Button Content="{Binding LocalizedResources.TimeZero, Source={StaticResource LocalizedStrings}}" Tag="0.00:00" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                    <Button Content="{Binding LocalizedResources.Time15mins, Source={StaticResource LocalizedStrings}}" Tag="0.00:15" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                    <Button Content="{Binding LocalizedResources.Time1hour, Source={StaticResource LocalizedStrings}}" Tag="0.01:00" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click"  />
                    <Button Content="{Binding LocalizedResources.Time1day, Source={StaticResource LocalizedStrings}}" Tag="1.00:00" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <Button Content="{Binding LocalizedResources.Time5mins, Source={StaticResource LocalizedStrings}}" Tag="0.00:05" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                    <Button Content="{Binding LocalizedResources.Time30mins, Source={StaticResource LocalizedStrings}}" Tag="0.00:30" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                    <Button Content="{Binding LocalizedResources.Time18hours, Source={StaticResource LocalizedStrings}}" Tag="0.18:00" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                    <Button Content="{Binding LocalizedResources.Time1week, Source={StaticResource LocalizedStrings}}" Tag="7.00:00" Style="{StaticResource BasePickerQuickButtonStyle}" Click="QuickButton_Click" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>

</controls:BasePickerPage>
